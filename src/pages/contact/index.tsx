import React from "react";

const Contact = () => {
    return (
        <div>
            <h1>Contact </h1>
        </div>
    )
}

export default Contact;

//trước khi muốn đẩy code lên GitHub
// - phải có source code
// - Tạo 1 repo tren GitHub để lưu code
// - vào source => "git init" => để khởi tạo GitHub trên source => và chỉ 
// thực hiện 1 lần duy nhất khi tạo mới 1 repo và remote trên source
